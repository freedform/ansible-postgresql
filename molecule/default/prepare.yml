---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Install python3-psycopg2
      ansible.builtin.apt:
        name: python3-psycopg2
        state: present
        update_cache: true
